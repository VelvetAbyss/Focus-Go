{
  "name": "@focus-go/desktop",
  "private": true,
  "version": "0.1.0-beta.5",
  "description": "Focus & Go desktop app (Tauri v2 shell wrapping @focus-go/web)",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "typecheck": "tsc -p tsconfig.json",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-process": "^2",
    "@tauri-apps/plugin-updater": "^2"
  }
}
